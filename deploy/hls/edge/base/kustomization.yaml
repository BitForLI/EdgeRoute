apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
configMapGenerator:
  - name: edge-config
    files:
      - nginx.conf
      - toxiproxy.json
generatorOptions:
  disableNameSuffixHash: true
